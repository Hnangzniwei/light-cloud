{
  "name": "light-cloud",
  "version": "1.0.0",
  "description": "A lightweight cloud service deployment project.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "git add . && git commit -m "deploy" && git push -u origin main",
    "destroy": "git checkout -b destroy && git push -u origin destroy"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/你的用户名/light-cloud.git"
  },
  "keywords": [
    "cloud",
    "deployment",
    "docker",
    "github-actions"
  ],
  "author": "你的名字",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",  // 示例依赖，根据实际需求添加
    "dotenv": "^10.0.0"    // 示例依赖，用于管理环境变量
  },
  "devDependencies": {
    "nodemon": "^2.0.7"  // 示例开发依赖，用于开发环境
  }
}
